spring:
  datasource:
    url: jdbc:postgresql://sitprepdb.chs8gaeg29qb.us-east-1.rds.amazonaws.com:5432/sitprep
    username: dione
    password: noaccess4UCorbino
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: statusnow
        globally_quoted_identifiers: true
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB
  mvc:
    throw-exception-if-no-handler-found: true
    async:
      request-timeout: 3600000
server:
  port: 8080
  error:
    path: /user/error
    whitelabel:
      enabled: false
